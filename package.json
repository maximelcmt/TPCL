{
  "name": "tpcl",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"cd backend && php artisan serve\" \"cd frontend && npm run dev\" --names \"Laravel,Nuxt\" --prefix-colors \"blue,green\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}

